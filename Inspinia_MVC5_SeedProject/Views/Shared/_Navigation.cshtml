<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    @if (Session["username"] != null)
                    {
                        <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                            <span class="clear">
                                <span class="block m-t-xs">
                                    <strong class="font-bold">@Session["username"]</strong>
                                </span>
                                <span class="text-muted text-xs block">Setting <b class="caret"></b></span>
                            </span>
                        </a>
                        <ul class="dropdown-menu animated fadeInRight m-t-xs">
                            <li>@Html.ActionLink("LogOut", "LogOut", "Home", routeValues: null, htmlAttributes: new {title = "Manage"})</li>
                        </ul>
                    }
                    else
                    {
                        <span class="clear">
                            <span class="block m-t-xs">
                                <strong class="font-bold">You Are Not Login</strong>
                            </span>
                        </span>
                    }
                </div>
                <div class="logo-element">
                    AB
                </div>
            </li>
            <li class="@Html.IsSelected(action: "Index")">
                <a href="@Url.Action("Index", "Home")">
                    <i class="fa fa-laptop"></i> <span class="nav-label">Manage</span><span class="fa arrow"></span>
                </a>
                <ul class="nav nav-second-level collapse">
                    <li class="@Html.IsSelected(action: "Index")">
                        <a href="@Url.Action("Index", "Branches")">
                            Branches
                        </a>
                    </li>
                    <li class="@Html.IsSelected(action: "Index")">
                        <a href="@Url.Action("Index", "Students")">
                            Students
                        </a>
                    </li>
                    <li class="@Html.IsSelected(action: "Index")">
                        <a href="@Url.Action("Index", "Users")">
                            Users
                        </a>
                    </li>
                    <li class="@Html.IsSelected(action: "Index")">
                        <a href="@Url.Action("Index", "Cours")">
                            Course
                        </a>
                    </li>
                    <li class="@Html.IsSelected(action: "Index")">
                        <a href="@Url.Action("Index", "Branches")">
                            Enrollment
                        </a>
                    </li>
                    <li class="@Html.IsSelected(action: "Index")">
                        <a href="@Url.Action("Index", "Branches")">
                            Result
                        </a>
                    </li>
                </ul>
            </li>
        </ul>
    </div>
</nav>